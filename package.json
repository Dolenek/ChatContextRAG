{
  "name": "chat-context-rag",
  "version": "0.1.0",
  "description": "Desktop app for archiving Discord and WhatsApp conversations in a local RAG database.",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "test": "node --test tests/*.test.js",
    "backend": "py -3.9 -m uvicorn backend.app:app --reload --port 8765"
  },
  "devDependencies": {
    "electron": "^39.8.5"
  },
  "dependencies": {
    "discord.js": "^14.26.5"
  },
  "overrides": {
    "undici": "6.27.0"
  }
}
